Question 4

1. Create Table

Orders Table:

CREATE TABLE Orders (
    order_id INT PRIMARY KEY,
    customer_id INT,
    product_id INT,
    quantity INT,
    total_amount INT
);

- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 

2. Insert Sample Data

Insert into Orders

INSERT INTO Orders VALUES
(1, 101, 201, 2, 2000),
(2, 102, 202, 1, 1500),
(3, 101, 203, 3, 3000),
(4, 103, 201, 5, 5000),
(5, 101, 202, 2, 2500),
(6, 102, 204, 4, 4000),
(7, 104, 205, 6, 6000),
(8, 101, 201, 1, 1200),
(9, 103, 203, 2, 2200);

- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 

3. SQL Queries

1. Find the total amount spent by each customer

SELECT customer_id, SUM(total_amount) AS total_spent
FROM Orders
GROUP BY customer_id;

- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 

2. Find the number of orders placed by each customer

SELECT customer_id, COUNT(order_id) AS number_of_orders
FROM Orders
GROUP BY customer_id;

- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 

3. Display customers who placed more than 3 orders

SELECT customer_id, COUNT(order_id) AS number_of_orders
FROM Orders
GROUP BY customer_id
HAVING COUNT(order_id) > 3;

- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 

4. Find customers whose total spending is greater than 10000

SELECT customer_id, SUM(total_amount) AS total_spent
FROM Orders
GROUP BY customer_id
HAVING SUM(total_amount) > 10000;

- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 

5. Find products whose total quantity sold is greater than 100

SELECT product_id, SUM(quantity) AS total_quantity
FROM Orders
GROUP BY product_id
HAVING SUM(quantity) > 100;