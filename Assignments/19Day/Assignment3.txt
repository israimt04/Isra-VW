Question 3


1.	Create Table

Employees Table:

CREATE TABLE Employees (
    emp_id INT PRIMARY KEY,
    emp_name VARCHAR(50),
    department VARCHAR(50),
    salary INT,
    joining_date DATE
);

- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 

2.	Insert Sample Data

Insert into Employees

INSERT INTO Employees VALUES
(1, 'Isra', 'IT', 60000, '2022-01-10'),
(2, 'Fatima', 'HR', 50000, '2021-03-15'),
(3, 'Omar', 'IT', 75000, '2020-06-20'),
(4, 'Aisha', 'Finance', 65000, '2019-09-12'),
(5, 'Zaid', 'IT', 55000, '2023-02-01'),
(6, 'Zorawar', 'Marketing', 52000, '2022-07-05');

- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 

3.	SQL Queries

1.	Find employees whose salary is greater than the average salary of all employees

SELECT *
FROM Employees
WHERE salary > (
    SELECT AVG(salary)
    FROM Employees
);

- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 

2.	Find employees whose salary is greater than the average salary of their own department

SELECT *
FROM Employees e
WHERE salary > (
    SELECT AVG(salary)
    FROM Employees
    WHERE department = e.department
);

- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 

3.	Find employees who earn the highest salary in their department

SELECT *
FROM Employees e
WHERE salary = (
    SELECT MAX(salary)
    FROM Employees
    WHERE department = e.department
);

- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 

4.	Display employees who earn less than the highest salary in the company but more than the average salary

SELECT *
FROM Employees
WHERE salary < (
    SELECT MAX(salary)
    FROM Employees
)
AND salary > (
    SELECT AVG(salary)
    FROM Employees
);

- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - 

5.	Find departments whose average salary is greater than the company’s average salary

SELECT department, AVG(salary)
FROM Employees
GROUP BY department
HAVING AVG(salary) > (
    SELECT AVG(salary)
    FROM Employees
);